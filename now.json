{
    "version": 2,
    "name": "frontend",
    "builds": [{ "src": "frontend/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "frontend/$1" }
    ]
}